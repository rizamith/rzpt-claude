# Fotos e ficheiros temporários — as imagens são efémeras, não entram no repositório
*.jpg
*.jpeg
*.png
*.heic
*.webp
/tmp/
/uploads/

# Bytecode do Python — gerado ao correr os scripts, nao e estado
__pycache__/
*.pyc

# Ruído de sistema
Thumbs.db
desktop.ini
.DS_Store

# Pasta de estágio para material a importar — o que fica é o registo escrito extraído
/import/
rzpt-claude.code-workspace
